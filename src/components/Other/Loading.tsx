import { FC } from 'react'

const Loading: FC = () => {
    return (
        <section className='text-center text-4xl font-bold flex items-center justify-center'>Загрузка...</section>
    )
}

export default Loading